name: CICD static web site
on: [push]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
       with:
          node-version: 25
      - run: npm ci
      - run: npm test
~
